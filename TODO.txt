работает га
работает операторы
работает вычисление цф
работает вычисление описанного объема сгенерированных цепей
работает генерация SCAD файлов
работает алгоритм трассировки
работает алгоритм первичной расстановки компонентов
работает кодирование хромосом-номер ноды в дереве, флаги свойств. Как задавать номер-адрес ноды?
???????
разбитие геометрии корпуса на октодерево. сохранение этого в файл.
Разбор STL //https://ru.wikipedia.org/wiki/STL_%28%D1%84%D0%BE%D1%80%D0%BC%D0%B0%D1%82_%D1%84%D0%B0%D0%B9%D0%BB%D0%B0%29
//простой формат файла-куча вершин.
парсинг схемы
парсинг конфигов. что в конфигах?
Тесты+
Log4j+
Git+
//шаг0. Загружаем STL корпуса.+
//Получаем список вершин.+
//Строим описывающий куб+
//записываем координаты точек диагонали.+
//Берем за 0+
//Строим из куба октодерово
//ответ на вопрос: находиться ли внутри октанта полигоны или пересекают ли они его?
//Ответ на 1 вопрос: если хотя бы одна координата внутри-серый.
//ответ на 2: если прямая, соединяющая вершины пересекает хотя бы одну сторону-серый.
//На какие вопросы нужно отвечать при построение октодерева? Есть ли в заданном объеме геометрия.
/*Нужно ли отличать внутренние полости от внешних. Как поступать с отверситями под крепеж или под конструктивные элементы?*/
//Определяем внутренние полости. Помечаем их как неперемещаемые.
//Определяем Находим самую большую ноду в корпусе.
//Распределяем в ней компоненты. Алгоритм распределения:?
    /*возможно так:
    *Проверяем влезет ли по объему-складываем объем всех деталей и сравниваем с объемом корпуса.
    *Если влазит - пихаем компоненты в порядке перечисления, пока не закончится объем.
    *Берем следующую пустую ноду. повторяем.
    *И так пока не закончиться место, либо детали.
    */
//Как описывать смещение?-адрес ноды + внутренее смещение от точки деления. Добовлять ли ноды?